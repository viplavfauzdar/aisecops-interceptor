# Codex Handoff

Repo:
- `/Users/viplavfauzdar/Projects/aisecops-interceptor`

Purpose:
- Use this file as baseline context for a fresh Codex session when the previous session became interruption-prone.
- Re-verify current git state before taking action. Do not assume branch pointers or worktree cleanliness from this file alone.

## Working Rules

- New Codex sessions must read `.codex-handoff.md` first.
- Update `.codex-handoff.md` at the end of any material task.
- Follow `AGENTS.md` strictly.
- Use short commands only.
- Check state after each step.
- Prefer `rg` for search.
- Use `apply_patch` for manual edits.
- Commit only after verification passes.
- Keep security logic centralized in interceptor and policy layers.
- Keep adapters thin.
- `aisecops_interceptor/core/context.py` is the single canonical home of `RuntimeContext`.

## Current Verified Git State

Verified on:
- 2026-06-03 12:20:12 EDT

At verification time:
- Current branch: `dev`
- Worktree: clean
- Latest `dev` commit: `c7ac5b5` (`feat: add MCP policy proxy foundation`)
- Remote `origin/dev`: `c7ac5b5`
- Local `main`: `d052615`
- Remote `origin/main`: `473af44` (`Merge dev into main for AISecOps v0.8.1 (#30)`)
- `dev...main`: `7 0`
- `origin/dev...origin/main`: `6 1`

Interpretation:
- `dev` contains `origin/main` v0.8.1, v0.9.0 Local Enforcement Mode, and v0.9.5 MCP Policy Proxy foundation.
- v0.8.1 is released per current project context.
- v0.9.5 MCP Policy Proxy foundation is implemented on `dev`.
- v0.9.5 status: ready to merge into `main` and tag `v0.9.5`.

## Recent Commits

- `c7ac5b5` `feat: add MCP policy proxy foundation`
- `3793f50` `Merge origin/main into dev after v0.8.1 release`
- `e920d4a` `feat: add local enforcement mode`
- `a18e01b` `docs: update agent workflow instructions`
- `1a92fe7` `docs: update codex handoff for runtime controls`
- `6686c85` `feat: add agent runtime controls`
- `473af44` `Merge dev into main for AISecOps v0.8.1 (#30)`
- `d052615` `Merge dev into main for AISecOps v0.8.0 foundation`
- `8374728` `docs: document structured plan extraction`
- `952e792` `fix: normalize extracted capability names`
- `d67cd15` `feat: add structured plan view to replay dashboard`
- `246c04b` `feat: add structured plan extraction foundation`
- `f6496d3` `docs: add codex handoff file`
- `ff0c50f` `added claude generated marketing strategy & plan`

## Durable Product Context

AISecOps Interceptor current positioning:
- runtime governance and forensics layer for AI agents

Known implemented capabilities before any new work:
- MCP Policy Proxy foundation
- MCP invocation and decision models
- MCP capability mapping
- MCP policy evaluation without execution
- MCP-specific audit metadata
- MCP replay metadata exposure
- local enforcement mode
- local CLI via `aisecops-local` and `python -m aisecops_interceptor.local.cli`
- replay-compatible local JSONL audit logs
- local default provenance for local agents
- safe/mock local tool execution for v0.9.0 foundation
- agent runtime controls
- runtime budgets
- tool call limits
- depth limits
- runtime seconds limits
- estimated cost limits
- `RuntimeBudget` / `RuntimeUsage`
- centralized heuristic cost estimator
- runtime usage in audit events
- budget status in replay APIs
- structured plan extraction
- risk scoring
- capability validation
- replay APIs
- Replay Audit UI support
- provenance-aware replay
- execution graphs
- structured audit events
- capability-gated execution
- runtime forensic investigation support

Known README/doc state before any future changes:
- latest verified local pytest run: `169 passed`
- README includes `Agent Runtime Controls`
- README includes `Local Enforcement Mode`
- README includes `MCP Policy Proxy`

Known v0.8.1 implementation notes:
- Runtime governance is backend/runtime only.
- Cost enforcement is heuristic only; no vendor billing APIs are integrated.
- Dashboard UI is owned by Claude Code and should not be modified unless explicitly instructed.

Known v0.9.0 implementation notes:
- Local Enforcement Mode foundation is backend/runtime/CLI/docs only.
- Local mode evaluates local tool calls through the existing interceptor, policy engine, capability registry, structured plan extraction, runtime budgets, and audit logger.
- Local audit logs are structured JSONL and replay-compatible with the existing replay engine.
- Non-dry-run local execution uses safe/mock tool behavior only; no unsafe shell execution or arbitrary filesystem actions were added.
- No dashboard UI changes were made.

Known v0.9.5 implementation notes:
- MCP Policy Proxy foundation is backend/runtime/CLI/docs only.
- It evaluates MCP tool invocations before execution and does not execute MCP calls.
- It reuses existing `AgentInterceptor`, `PolicyEngine`, `CapabilityRegistry`, runtime budgets, `AuditLogger`, and replay engine.
- MCP audit events include `protocol`, `client_id`, `server_name`, `tool_name`, `decision`, and `capability`.
- No network listener or full MCP gateway was added.
- No dashboard UI changes were made.

## Roadmap

- v0.8.1: Agent Runtime Controls — released.
- v0.9.0: Local Enforcement Mode foundation — implemented on `dev`.
- v0.9.5: MCP Policy Proxy foundation — implemented on `dev`, ready to merge into `main` and tag `v0.9.5`.
- Next: v1.0.0 Runtime Governance Platform.

## Repo-Specific Architecture Notes

Runtime execution contract:
- Prompt
- Prompt Guard (input inspection)
- Guarded LLM Pipeline
- Output Guard
- RuntimeContext construction
- AgentInterceptor
- PolicyEngine evaluation
- Approval workflow (if required)
- ExecutionGate
- Tool execution
- unified runtime event logging

Shared model locations:
- `aisecops_interceptor/core/context.py` → `RuntimeContext`
- `aisecops_interceptor/core/models.py` → shared runtime dataclasses / Pydantic models
- `aisecops_interceptor/core/events.py` → unified runtime event model

## Verification Commands

Preferred Python test command:
```bash
./.venv/bin/python -m pytest -q
```

Additional verification when touching core runtime logic:
```bash
./.venv/bin/python -m compileall aisecops_interceptor examples tests
./.venv/bin/python -m examples.agent_demo
rg "^class RuntimeContext" aisecops_interceptor examples tests
```

## First-Step Checklist For Fresh Session

Run these first:
```bash
git branch --show-current
git status --short
git log --oneline --decorate --max-count=8 --all
git rev-list --left-right --count dev...main
git rev-list --left-right --count origin/dev...origin/main
```

Then:
- compare actual state with this handoff
- call out any drift before editing
- wait for or proceed with the user’s next task

## Important Session Note

The previous session was getting interrupted when the user switched focus. Because of that:
- avoid long-running chained commands
- checkpoint often
- prefer smaller verification steps over one large batch when practical
